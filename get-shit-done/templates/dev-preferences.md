---
description: 将开发者偏好加载到本次会话中
---

# 开发者偏好

> 由 GSD 于 {{generated_at}} 从 {{data_source}} 生成。
> 运行 `/gsd:profile-user --refresh` 以重新生成。

## 行为指令

在与该开发者协作时请遵循以下指令。高置信度的指令应直接应用。低置信度的指令应以试探性方式尝试（"根据您的偏好配置，我将尝试 X——如果不对请告诉我"）。

{{behavioral_directives}}

## 技术栈偏好

{{stack_preferences}}
